<%
    const Card = require('./card.ejs');
%>


<% news.forEach((item) => { %>
    <%-
        Card({
            title: item.title,
            description: item.description
        });
    %>
<% }); %>
